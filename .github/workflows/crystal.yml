name: Crystal CI
on: [push]
jobs:
    run-tests:
      runs-on: macos-10.15
      steps:
      - uses: actions/checkout@v2
      # - name: Install dependencies
      #   run: shards install
      - name: Run tests
        run: crystal spec

# name: Crystal CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   build:

#     runs-on: macos-latest

#     container:
#       image: crystallang/crystal

#     steps:
#     - uses: actions/checkout@v2
#     - name: Install dependencies
#       run: shards install
#     - name: Run tests
#       run: crystal spec
