version: '3.1'
services:
  db:
    image: postgres
    ports: 
      - 5430:5430
    environment: 
      - POSTGRES_DB=crystal_todo
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      
